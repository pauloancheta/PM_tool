<h3><%= @task.title %></h3>
<p><%= @task.due_date %></p>

<%= link_to "EDIT", edit_task_path(@task), class: "btn btn-default" %>
<%= link_to "DELETE", task_path(@task), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-default" %>